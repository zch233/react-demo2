import React from 'react';
import { Wrapper } from './FilterStyles';

const FilterBar: React.FC = () => {
  return <Wrapper className={'pageWidthWithCenter'}></Wrapper>;
};

export default FilterBar;
